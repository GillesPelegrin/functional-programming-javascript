/* Is it possible to put the responsibility here which function are called from this flow ? 


Controller.addApplicationService(service).addRepository(repository)

And that the functions only can reach this content inside.

*/ 